# kotlin-lotto

## 기능목록

### 1. 구입금액을 입력받는다
로또 1장 : 1000원
- 시스템 입력을 통해 구입금액을 입력받는다.
  - 입력 : 구입금액을 입력해 주세요.
- 입력한 구입금액에 맞는 로또 갯수를 출력한다
  - OO개를 구매했습니다.


### 2. 구입금액에 만큼 로또 번호 자동으로 생성한다
- 1 ~ 45 사이의 중복되지 않는 숫자 6개를 랜덤으로 뽑아 로또 번호를 생성한다.
- 생성된 로또 번호를 출력한다
  - [1, 2, 3, 4, 5, 6]

### 3. 지난주 당첨번호를 입력받는다
- 시스템 입력을 통해 지난주 당첨번호 6개를 입력받는다.
  - 입력 : 지난 주 당첨번호를 입력해 주세요.

### 4. 당첨통계를 낸다
- 아래 기준에 따라 당첨결과를 출력한다
  - 3개 일치 : 5,000원
  - 4개 일치 : 50,000원
  - 5개 일치 : 1,500,000원
  - 6개 일치 : 2,000,000,000원
- 수익률을 계산하여 출력한다 
  - 수익률(%) : 총 당첨금 / 구입금액
  - 소수점 둘째 자리까지 출력한다

## 프로그래밍 요구 사항
- 모든 기능을 TDD로 구현해 단위 테스트가 존재해야 한다. 단, UI(System.out, System.in) 로직은 제외
  - 핵심 로직을 구현하는 코드와 UI를 담당하는 로직을 구분한다.
  - UI 로직을 InputView, ResultView와 같은 클래스를 추가해 분리한다.
- indent(인덴트, 들여쓰기) depth를 2를 넘지 않도록 구현한다. 1까지만 허용한다.
    - 예를 들어 while문 안에 if문이 있으면 들여쓰기는 2이다.
  - 힌트: indent(인덴트, 들여쓰기) depth를 줄이는 좋은 방법은 함수(또는 메서드)를 분리하면 된다.
- 함수(또는 메서드)의 길이가 15라인을 넘어가지 않도록 구현한다.
- 함수(또는 메서드)가 한 가지 일만 잘 하도록 구현한다.

## 추가된 요구 사항
- 모든 기능을 TDD로 구현해 단위 테스트가 존재해야 한다. 단, UI(System.out, System.in) 로직은 제외
  - 핵심 로직을 구현하는 코드와 UI를 담당하는 로직을 구분한다.
  - UI 로직을 InputView, ResultView와 같은 클래스를 추가해 분리한다.
- 함수(또는 메서드)의 길이가 10라인을 넘어가지 않도록 구현한다.
  - 함수(또는 메서드)가 한 가지 일만 하도록 최대한 작게 만들어라.
- 배열 대신 컬렉션을 사용한다.
- Enum 클래스를 적용해 프로그래밍을 구현한다.
- 모든 원시 값과 문자열을 포장한다
- 줄여 쓰지 않는다(축약 금지).
- 일급 컬렉션을 쓴다.

## 학습한 것
- 🔥 companion object란?
 - 클래스 내부에 딱 1개만 둘 수 있는 특별한 object
 - 이 안에 선언된 멤버는 클래스명으로 바로 접근 가능
 - Java에서 말하는 static 멤버처럼 동작하지만, 사실은 companion object 내부의 함수와 변수는 클래스 로딩 시 생성되는 싱글톤 객체
