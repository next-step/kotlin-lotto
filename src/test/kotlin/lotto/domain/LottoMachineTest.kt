package lotto.domain

internal class LottoMachineTest {
    // LottoProfitRateTest 에서 테스트
    // @ParameterizedTest
    // @ValueSource(ints = [500, 10, 0, 50, 100, 99, 999])
    // fun `1000원 이하로 구매를 시도하면 예외가 발생한다`(price: Int) {
    //     assertThrows<IllegalArgumentException> {
    //         val manualLottoNumbers = ManualNumbers(listOf())
    //         val buyingLotto = BuyingLotto(LottoPrice(price), manualLottoNumbers)
    //
    //         LottoMachine.buy(buyingLotto)
    //     }
    // }
}
