package lotto.ui

import org.junit.jupiter.api.Test

internal class ResultViewTest {

    @Test
    fun showLottoTickets() {
    }

    @Test
    fun showMatchResult() {
        // // given
        // val money = 14000
        // val result = mapOf(3 to 1, 4 to 0, 5 to 0, 6 to 0)
        // val expectedMessage = "3 개 일치(5000 원) - 1 개"
        //
        // // when
        // val resultMessage = ResultView().showMatchResult(money, result)
        //
        // // then
        // Assertions.assertThat(resultMessage).has(expectedMessage)
    }
}
